version: '3'
services:
    redis:
        image: redis
        container_name: cache
        expose:
            - 6379

    api:
        build: ./api/
        links:
            - redis
        environment:
            - REDIS_URL=redis://cache
        ports:
            - 8080:8080
